fnction capitalizeFLetter() {
	let string = 'sanket';
	console.log(string.replace(/^./, string[0].toUpperCase()));
}
	
	capitalizeFLetter()
	
	function capitalizeFLetter(){
		let string="Sanket";
		
		console.log(string.replace(/^./, string.toUpperCase()));
	}
	
	capitalizeFLetter()